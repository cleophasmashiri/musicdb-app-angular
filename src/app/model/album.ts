export class Album {
    constructor(public id: number, public title: string, public release_date: string, public cover: string, public cover_small: string, 
        public cover_medium: string, public cover_big: string) {}
}

//item.id, item.title, item.year, item.cover, item.cover_small, item.cover_medium, item.cover_big,